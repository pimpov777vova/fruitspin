# 🛡️ VPN Telegram Bot

Telegram бот для продажи VPN с оплатой через ЮKassa, CryptoBot и TON Connect.

## 🚀 Быстрая установка

### Вариант 1: Автоматическая установка (рекомендуется)

```bash
# Скачайте и запустите установщик
curl -sSL https://raw.githubusercontent.com/your-repo/vpn-bot/main/install.sh | bash
```

### Вариант 2: Ручная установка

1. Скопируйте файлы на сервер
2. Выполните команды:

```bash
# Установка пакетов
apt update && apt install -y python3 python3-pip python3-venv

# Создание директории
mkdir -p /opt/vpn-bot && cd /opt/vpn-bot

# Скопируйте bot.py и .env в эту папку

# Создание окружения
python3 -m venv venv
source venv/bin/activate
pip install aiogram aiohttp yookassa cryptography python-dotenv aiosqlite

# Запуск
python bot.py
```

## 📁 Файлы

- `install.sh` - Скрипт автоматической установки
- `bot.py` - Основной код бота (создаётся установщиком)
- `.env.example` - Пример конфигурации
- `README.md` - Этот файл

## ⚙️ Настройка

1. Создайте бота у @BotFather
2. Получите токены платёжных систем
3. Настройте VPN сервер (3X-UI)
4. Запустите установщик

## 📞 Поддержка

Смотрите файл `инструкция.txt` в корне проекта.
